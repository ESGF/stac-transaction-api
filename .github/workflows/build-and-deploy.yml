name: stac-transaction-api CI/CD

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - integration
  # pull_request:
  #   branches:
  #     - main

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: transaction-api
  CONTAINER_NAME: transaction-api
  ECS_TASK_DEFINITION: ecs/task-definition.json

permissions:
  id-token: write
  contents: read

jobs:
  # =========================
  # BUILD (always safe)
  # =========================
  build:
    runs-on: ubuntu-latest

    outputs:
      image-uri: ${{ steps.export.outputs.image-uri }}

    if: |
      (
        github.ref == 'refs/heads/add-ci-cd' ||
        github.ref == 'refs/heads/integration' ||
        github.ref == 'refs/heads/main'
      )

    steps:
    - uses: actions/checkout@v4

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-region: ${{ env.AWS_REGION }}
        role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}

    - name: Login to Amazon ECR
      id: ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Docker metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: |
          ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-

    - name: Build & push image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./Dockerfile
        platforms: linux/x86_64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

    - name: Export image URI
      id: export
      run: |
        IMAGE_URI=$(echo "${{ steps.meta.outputs.tags }}" | head -n 1)
        echo "image-uri=$IMAGE_URI" >> $GITHUB_OUTPUT
        echo "$IMAGE_URI" > image-uri.txt

    - name: Upload image artifact
      uses: actions/upload-artifact@v4
      with:
        name: image-uri
        path: image-uri.txt

  # =========================
  # DEPLOY → INTEGRATION
  # =========================
  deploy-integration:
    runs-on: ubuntu-latest
    needs: build
    # environment: integration        # ← pulls AWS_ROLE_TO_ASSUME from the
    #                                 #   integration environment's secrets

    if: github.ref == 'refs/heads/integration'

    env:
      ECS_CLUSTER: esgf2-cluster-integration
      ECS_SERVICE: transaction-api

    steps:
    - uses: actions/checkout@v4

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-region: ${{ env.AWS_REGION }}
        role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
        # Resolves to the integration role ARN from the
        # integration environment secret

    - uses: actions/download-artifact@v4
      with:
        name: image-uri

    - run: echo "IMAGE_URI=$(cat image-uri.txt)" >> $GITHUB_ENV

    - name: Render task definition
      id: render
      uses: aws-actions/amazon-ecs-render-task-definition@v1
      with:
        task-definition: ${{ env.ECS_TASK_DEFINITION }}
        container-name: ${{ env.CONTAINER_NAME }}
        image: ${{ env.IMAGE_URI }}

    - name: Deploy to Integration
      uses: aws-actions/amazon-ecs-deploy-task-definition@v2
      with:
        cluster: ${{ env.ECS_CLUSTER }}
        service: ${{ env.ECS_SERVICE }}
        task-definition: ${{ steps.render.outputs.task-definition }}
        wait-for-service-stability: true

  # =========================
  # DEPLOY → PRODUCTION
  # =========================
  deploy-production:
    runs-on: ubuntu-latest
    needs: build
    environment: production         # ← pulls AWS_ROLE_TO_ASSUME from the
                                    #   production environment's secrets

    if: github.ref == 'refs/heads/main'

    env:
      ECS_CLUSTER: esgf2-cluster-production
      ECS_SERVICE: transaction-api

    steps:
    - uses: actions/checkout@v4

    - name: Configure AWS credentials (OIDC)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-region: ${{ env.AWS_REGION }}
        role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
        # Resolves to the production role ARN from the
        # production environment secret

    - uses: actions/download-artifact@v4
      with:
        name: image-uri

    - run: echo "IMAGE_URI=$(cat image-uri.txt)" >> $GITHUB_ENV

    - name: Render task definition
      id: render
      uses: aws-actions/amazon-ecs-render-task-definition@v1
      with:
        task-definition: ${{ env.ECS_TASK_DEFINITION }}
        container-name: ${{ env.CONTAINER_NAME }}
        image: ${{ env.IMAGE_URI }}

    - name: Deploy to Production
      uses: aws-actions/amazon-ecs-deploy-task-definition@v2
      with:
        cluster: ${{ env.ECS_CLUSTER }}
        service: ${{ env.ECS_SERVICE }}
        task-definition: ${{ steps.render.outputs.task-definition }}
        wait-for-service-stability: true